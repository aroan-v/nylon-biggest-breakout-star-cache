# Node modules (don't commit all packages)
node_modules/

# Environment variables (credentials, API keys, Firebase config)
.env

# Puppeteer Chromium downloads (if using default install)
.puppeteer/

# macOS metadata files
.DS_Store

# VSCode settings (optional)
.vscode/

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Firebase local emulators (if used)
firebase-debug.log
firestore-debug.log
.firebaserc

# Compiled output (if you decide to bundle anything)
dist/
build/


serviceAccountKey.json